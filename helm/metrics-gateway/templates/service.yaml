apiVersion: v1
kind: Service
metadata:
  name: metrics-gateway
spec:
  type: {{ .Values.service.type }}
  selector:
    app: metrics-gateway
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8080
